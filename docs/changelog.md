# Changelog

## Unreleased

- Changed the default highlighting mode for new installs to **On page** (`showHighlighting = 3`), while preserving existing users' explicit highlighting choices during settings migration.
